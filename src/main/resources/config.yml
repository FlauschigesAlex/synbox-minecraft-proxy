# SynboxProxy Configuration

# API Key for authentication
api_key: ""

# Organization identifier
organization: ""

# Register servers in velocity
register_server: true

# Refresh interval in seconds
refresh_interval: 300

# webserver port
webserverPort: 8084